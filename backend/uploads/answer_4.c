#include <stdio.h>

int main() {
    int a, b;

    // Read two integers from input
    scanf("%d %d", &a, &b);

    // Check if a is equal to b.
    if (a == b) {
        printf("a is equal to b.\n");
    }

    return 0;
}